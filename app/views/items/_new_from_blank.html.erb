<%= form_for(Item.new, html: {class: "form-horizontal"}) do |f| %>
  <%= render_item_form_fields(f) %>
  <div class="control-group">
    <div class="controls">
      <%= submit_tag "Save", class: "btn" %>
    </div>
  </div>
<%- end -%>